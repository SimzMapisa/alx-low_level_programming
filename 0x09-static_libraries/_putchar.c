#include "main.h"
#include <stdio.h>

/**
 * _putchar - outputs characters and strings
 * @c: string
 *
 * Description: "outputs characters and strings"
 *
 * Return: 0 (success)
 */

int _putchar(char c)
{
	return (putchar(c));
}

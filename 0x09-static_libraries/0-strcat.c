#include "main.h"
#include <string.h>

/**
 * _strcat - concatnates strings
 * @dest: pointer
 * @src: pointer that hold src str
 *
 * Description: "concats strings"
 *
 * Return: 0 (success)
 */

char *_strcat(char *dest, char *src)
{
	return (strcat(dest, src));
}

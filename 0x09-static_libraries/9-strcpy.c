#include "main.h"
#include <string.h>

/**
 * _strcpy - copy a string
 * @dest: destination
 * @src: source
 *
 * Description: "copy a string"
 *
 * Return: char
 */

char *_strcpy(char *dest, char *src)
{
	return (strcpy(dest, src));
}

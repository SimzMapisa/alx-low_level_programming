#include "main.h"
#include <ctype.h>

/**
 * _isalpha - checks if passed char is alphabetic
 * @c: char
 *
 * Description: "checks if passed char is alphabetic"
 *
 * Return: 0 (success)
 */

int _isalpha(int c)
{
	return (isalpha(c));
}

#include "main.h"
#include <stdio.h>

/**
 * _puts - output of chars
 * @s: pointer to chars
 *
 * Description: "output of characters and strings"
 *
 * Return: nothing
 */

void _puts(char *s)
{
	puts(s);
}

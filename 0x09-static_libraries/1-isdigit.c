#include "main.h"
#include <ctype.h>

/**
 * _isdigit - checks if a char is digit
 * @c: passed character
 *
 * Descrition: "checks if passed char is digit"
 *
 * Return: 0 (success)
 */

int _isdigit(int c)
{
	return (isdigit(c));
}

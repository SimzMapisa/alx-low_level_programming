#include "main.h"
#include <stdlib.h>

/**
 * _abs - calcualates the absolute value of a number
 * @n: number
 *
 * Description: "calcualates the absolute value of a number"
 *
 * Return: int absolute value
 */

int _abs(int n)
{
	return (abs(n));
}

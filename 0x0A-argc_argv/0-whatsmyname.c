#include <stdio.h>

/**
 * main - entry point
 * @argc: length of array of strings
 * @argv: array of arguments
 *
 * Description: "Entry point of the program"
 *
 * Return: Always 0 (success)
 */

int main(int argc, char *argv[])
{
	(void) argc;

	printf("%s\n", argv[0]);
	return (0);
}
